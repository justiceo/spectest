export default {
  startCmd: 'npm run start',
  baseUrl: 'https://localhost:8080',
  testDir: './test',
  filePattern: '\\.spectest\\.',
  rps: Infinity,
  timeout: 60000,
  randomize: false,
  happy: false,
  filter: '',
  runningServer: 'reuse',
  userAgent: 'chrome_windows',
  proxy: '',
};
